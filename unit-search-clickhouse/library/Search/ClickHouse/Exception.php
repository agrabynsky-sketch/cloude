<?php

/**
 * Код исключения:
 *   400 — неверные параметры поиска (можно показать пользователю);
 *   503 — ClickHouse недоступен (curl/сеть);
 *   прочие (обычно 500) — ошибка ClickHouse, текст ошибки сервера в сообщении (наружу не отдавать).
 */
class Search_ClickHouse_Exception extends Exception {
}
